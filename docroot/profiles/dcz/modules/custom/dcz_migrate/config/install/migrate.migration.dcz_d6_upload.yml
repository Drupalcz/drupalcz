id: dcz_d6_upload
label: File uploads
migration_group: upload
migration_tags:
  - Drupal 6
source:
  plugin: dcz_d6_upload
  node_type:
    - blog
    - story
    - forum
process:
  nid: nid
  vid: vid
  type: type
  field_attachment:
    plugin: iterator
    source: upload
    process:
      target_id:
        plugin: migration
        migration: dcz_d6_file
        source: fid
      display: list
      description: description
destination:
  plugin: entity:node
migration_dependencies:
  required:
    # @fixme Uncomment
    # po odkomentovani musi byt soubory naimportovany vsechny, to je pak podminka
    # - dcz_d6_file
    - dcz_d6_node:*
    # - d6_upload_field_instance
